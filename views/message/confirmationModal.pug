button.btn.btn-primary.mr-1(type='button' data-toggle='modal' data-target='#confirmation' onclick='preview(event, "confirmation-display")') Submit
button.btn.btn-default(type='button', id='preview-btn', onclick='preview(event, "preview-display")') Preview
#confirmation.modal.fade(tabindex="-1" role="dialog" aria-labelledby="confirmation-label" aria-hidden="true")
  .modal-dialog(role="document")
    .modal-content
      .modal-header
        h5#confirmation-label.modal-title Confirm
        button.close(type="button" data-dismiss="modal" aria-label="Close")
          span(aria-hidden="true") ×
      .modal-body
        p Are you sure you want to submit? Below is a preview of the display.
        div#confirmation-display
      .modal-footer
        button.btn.btn-secondary(type="button" data-dismiss="modal") Close
        button.btn.btn-primary(type="submit") Submit
